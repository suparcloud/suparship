---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: voiceai-livekit-self-hosted-env
spec:
  refreshInterval: 1m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect-voiceai
  target:
    name: voiceai-livekit-self-hosted-env  # name of the k8s Secret to be created
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: voiceai-livekit-self-hosted-env
