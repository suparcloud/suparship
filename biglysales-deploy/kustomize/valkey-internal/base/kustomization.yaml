apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- service.yaml
- statefulset.yaml
- pdb.yaml

commonLabels:
  app.kubernetes.io/part-of: biglysales-voiceai
  app: valkey-internal
