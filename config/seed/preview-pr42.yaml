# Seed: demo preview environment (pr-42 for demo/hello).
# Applied by: hack/seed.sh  (kubectl apply -f config/seed/)
# Matches the fake/in-memory seed in internal/fake/seed.go.
apiVersion: v1
kind: ConfigMap
metadata:
  name: suparship-preview-pr-42
  namespace: suparship-system
  labels:
    suparship.io/type: preview
    suparship.io/project: demo
data:
  preview.yaml: |
    apiVersion: suparship.io/v1alpha1
    kind: Preview
    metadata:
      name: pr-42
      createdAt: "2026-01-01T00:00:00Z"
    spec:
      project: demo
      service: hello
