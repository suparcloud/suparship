apiVersion: v2
name: color-app
description: A suparship demo chart — displays a color for promotion pipeline demos
type: application
version: 1.0.0
appVersion: "1.0.0"
annotations:
  # Canonical helmvalues schema version this chart targets.
  # Source: internal/helmvalues.SchemaVersion. Bump when migrating to a
  # new major schema; the publisher uses this for compat shims.
  suparship.io/values-schema-version: v1
dependencies:
  - name: suparship-common
    version: "0.1.0"
    repository: "file://../../../charts/lib/suparship-common"
