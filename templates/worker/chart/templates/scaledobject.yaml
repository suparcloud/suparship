{{- if .Values.autoscaling.enabled -}}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "worker.fullname" . }}
  labels:
    {{- include "worker.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    kind: Deployment
    name: {{ include "worker.fullname" . }}
  minReplicaCount: {{ .Values.autoscaling.minReplicaCount }}
  maxReplicaCount: {{ .Values.autoscaling.maxReplicaCount }}
  {{- with .Values.autoscaling.cooldownPeriod }}
  cooldownPeriod: {{ . }}
  {{- end }}
  {{- with .Values.autoscaling.pollingInterval }}
  pollingInterval: {{ . }}
  {{- end }}
  triggers:
    {{- if .Values.autoscaling.triggers }}
    {{- toYaml .Values.autoscaling.triggers | nindent 4 }}
    {{- else }}
    # cpu/memory is only a FALLBACK for workers — the meaningful trigger is
    # almost always queue length (rabbitmq, kafka, redis-streams, sqs, ...).
    # Set autoscaling.triggers via the values editor to scale on real work.
    - type: cpu
      metricType: Utilization
      metadata:
        value: {{ .Values.autoscaling.cpuTarget | quote }}
    - type: memory
      metricType: Utilization
      metadata:
        value: {{ .Values.autoscaling.memoryTarget | quote }}
    {{- end }}
  {{- with .Values.autoscaling.behavior }}
  advanced:
    horizontalPodAutoscalerConfig:
      behavior:
        {{- toYaml . | nindent 8 }}
  {{- end }}
{{- end }}
